# Workspace protocol

This is the room default, and it is in force now. A repository that needs different rules
provides `WORKSPACE_PROTOCOL.md` at its root: read it, follow it wherever it speaks to a
point, and keep what follows where it is silent. Quote what bears on a brief into the brief
rather than handing this layer to a Peer.
